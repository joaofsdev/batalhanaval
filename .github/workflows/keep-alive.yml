name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # A cada 14 minutos
  workflow_dispatch:  # Permite rodar manualmente

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s https://batalhanaval-te2c.onrender.com/api/games/fleet-config > /dev/null
